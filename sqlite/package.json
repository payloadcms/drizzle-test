{
  "name": "drizzle-sqlite-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn rimraf db.sqlite && ts-node index.ts",
    "generate": "drizzle-kit generate:sqlite --out migrations --schema schema.ts"
  },
  "dependencies": {
    "better-sqlite3": "^8.4.0",
    "drizzle-orm": "^0.27.0",
    "payload": "^1.11.2",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.4",
    "drizzle-kit": "^0.19.3",
    "typescript": "^5.1.6"
  }
}
